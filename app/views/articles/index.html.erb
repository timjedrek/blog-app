<%= render "header" %>

<div class="splash-container">
    <div class="splash">
        <%#<h1 class="splash-head">This is my blog</h1>%>
        <p class="splash-subhead">
            I used Ruby on Rails to complete a simple blog app which has create, read, update, destroy (CRUD) and basic user authentication functionality for "blog posts."
        </p>
        <p>
            <a href="#articlesgohere" class="tj-button tj-button-primary">Let's look at some blog posts!</a>
            <%= link_to 'Write a new article', new_article_path, :class => 'tj-button tj-button-secondary ' %>
        </p>
    </div>
</div>

<div class="content-wrapper" id="articlesgohere">
 
<br><br>
<div class="tj-g">
    <% @articles.reverse_each do |article| %>
      <div class="tj-u-lg-1-3 tj-u-1 l-box">
        <div class="t-box">
          <h3 class="content-subhead">
            <b><%= article.title %></b>
          </h3>
          <div>
            <%= truncate(article.text, length: 100) %>
            <br><br>
          </div>
          <div class="right-align">
            <%= link_to 'Read More', article_path(article), :class => 'tj-button tj-button-primary ' %>
          </div>
        </div>
      </div>
    <% end %>

</div>


<%= render "footer" %>